### **1\. 核心检索范式：猴子爬树（确定性遍历）**

* **不再是 search(query)，而是 explore(node\_id)。**  
* **操作流：** LLM 调用工具 explore("root") \-\> 拿到树根和几个主干分叉的简介 \-\> LLM 判断目标在分叉 B \-\> 调用 explore("branch\_B\_id") \-\> 拿到更细的叶子节点简介 \-\> 直到找全所有需要的信息。  

