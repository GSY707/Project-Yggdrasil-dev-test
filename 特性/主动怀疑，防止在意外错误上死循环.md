### **2\. 熔断机制：主动怀疑信息的正确性**

需要在 System Prompt 中为 Agent 植入一个\*\*“认知置信度（Confidence Threshold）”\*\*的自我审查机制：

* **Prompt 植入：** “当你连续两次使用同一说明书执行同一工具失败时，你必须怀疑说明书的准确性。此时停止死磕，你可以选择调用 ask\_human 工具向用户确认，或者调用 read\_file 工具直接去读取底层的物理源码以逆向核对参数。”  
* 这样，错误处理不再是单纯的代码逻辑，而变成了 Agent 思考流（Chain of Thought）的一部分。